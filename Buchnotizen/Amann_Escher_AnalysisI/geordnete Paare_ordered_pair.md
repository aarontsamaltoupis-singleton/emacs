aus zwei Objekten $a$ und $b$ wird ein neues Objekt, das **geordnete Paar** gebildet:

Definition der Gleichheit zweier geordneter Paare:
$(a,b) = (a',b') :\iff(a=a')\land (b=b')$

a: erste Komponente, b: zweite Komponente

Sei $x=(a,b)$
$pr_{1}(x):=a, \ pr_{2}(x):=b$

$pr_{j}(x)$ ist die **j-te Projektion** von $=(a,b)$, wobei in dem Fall gilt $j\in \{ 1,2 \}$



# Definition durch Mengen
$(m,n):=\{ m ,\{ m,n \}\}$


#Tao_Analysis1 
# Definition 3.5.1 (Ordered Pair)
If $x$ and $y$ are any objects, we define the *ordered pair* (x,y) to be a new object, consisting of x as its first component and y as itss second component.
