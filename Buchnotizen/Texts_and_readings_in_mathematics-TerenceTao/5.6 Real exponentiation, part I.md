#Tao_Analysis1

# Definition 5.6.1 (Exponentiating a real by a natural number)
Let x be a real number.
    To raise x to the power 0, we define $x^0:=1$.
    Now suppose recursively that $x^n$ has been defined for some natural number n, 
    then we define $x^{n+1}:=x^n\cdot x$.
   
# Definition 5.6.2 (Exponentiating a real by an integer)
$\forall -n\in\mathbb{Z}^-: x^{-n}:=-\frac{1}{x^n}$


# Definition 5.6.4  (Definition of nth squareroot)
Let $x\geq 0$ be a non-negative real and let $n\geq 1$ be a positive integer. 
We define $x^{\frac{1}{n}}$ by the formula

$x^{\frac{1}{n}}:=\sup\{y\in\mathbb{R}:y\geq 0 \land y^n\leq x\}$

# Lemma 5.6.5 Existence of nth roots
Let $x\geq 0$ be a non-negative real and let $n\geq 1$ be a positive integer. 
$E:=\{y\in R: y\geq 0 \land y^n\leq x\}$ is non-empty and bounded above.
Also: $x^{\frac{1}{n}}$ is a real number.

- Es muss hier nur gezeigt werden, dass E beschraenkt ist, aufgrund der  [[Buchnotizen/Texts_and_readings_in_mathematics-TerenceTao/5.5 the least upper bound property|least upper bound property]]


# Definition 5.6.7 (rational Exponent)
$x^{q}:=(x^{1/b})^{a}$


