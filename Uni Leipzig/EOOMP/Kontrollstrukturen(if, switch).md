# Bedingte Anweisungen
## if

	if (a<b) {
		c=a;	
	} else {
		c=b;
	}


# if else

	if (a==1) {
		c=b;	
	} else if(a==2) {
		c=b;
	}


# switch
Der Fall wird ausgeführt, dessen Wert a hat
Default wird ausgeführt, wenn a einen Wert hat, für den es keinen case gibt.


	switch (a) {
		case 1: 
			<>
			break
		case 2:
			<>
			break
		case 3:
		default:
			<>
	}




